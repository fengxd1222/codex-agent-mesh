{
  "name": "@codex-agent-mesh/mcp-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.0",
    "ajv": "8.20.0",
    "zod": "4.4.3"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && esbuild src/index.ts src/native-transport.ts --bundle --platform=node --format=esm --target=node22 --outdir=dist/bundle --entry-names=[name] --log-level=warning && node scripts/copy-protocol-schema.mjs",
    "lint": "eslint src test",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "npm run build && node --test test/*.test.mjs",
    "start": "node dist/index.js"
  }
}
