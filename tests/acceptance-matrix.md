# Acceptance matrix

The machine-readable source is `tests/acceptance-matrix.json`. `npm run protocol:check` rejects missing, duplicate, malformed, or unclassified AC entries. Live rows are opt-in and are not run by offline CI.
